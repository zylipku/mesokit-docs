(kdv-1d)=
# `kdv_1d` — Korteweg–de Vries equation

**Model.** The KdV equation

$$
\partial_t u + 6\, u\, \partial_x u + \partial_{xxx} u = 0,
\qquad x \in [0, L_x) \text{ periodic},
$$

balances nonlinear steepening against third-order dispersion. It is a
completely integrable Hamiltonian system with infinitely many conserved
quantities (mass $\int u$, momentum $\int u^2$, energy
$\int [\tfrac12 u_x^2 - u^3]$, …) and soliton solutions that pass through one
another unchanged. Unlike the two phase-field equations it is not a
gradient flow, so this dataset is the toolkit's example of *reversible*
dynamics — a model that learns a dissipative structure should find nothing
to dissipate here.

**Numerical scheme** (Dedalus v3, Fourier–Galerkin in space, SBDF2 in time).

1. *Spatial discretisation.* As for Allen–Cahn: real Fourier basis on `Nx`
   points, $\partial_x \mapsto ik$, nonlinear products evaluated on the
   `dealias` $\times$ `Nx` grid. The KdV nonlinearity is quadratic, for
   which a padding factor $3/2$ would already be alias-free; the factor $2$
   is kept for uniformity with Allen–Cahn.
2. *Implicit/explicit split.* The equation is posed as
   $\partial_t u + \partial_{xxx} u = \mathcal{F}(u)$ with
   $\mathcal{F}(u) = -3\,\partial_x(u^2)$ (identical to $-6u\,\partial_x u$):
   the dispersive operator $\mathcal{L} = \partial_{xxx} \mapsto (ik)^3 = -ik^3$
   is implicit, the convective flux explicit and evaluated as
   $\hat{\mathcal F}_k = -3ik\,\widehat{u^2}_k$.
3. *Time stepping.* SBDF2 with the purely imaginary implicit symbol,

   $$
   \hat u^{n+1}_k = \frac{4\hat u^n_k - \hat u^{n-1}_k + 2\Delta t\,(2\hat{\mathcal F}^n_k - \hat{\mathcal F}^{n-1}_k)}{3 - 2i\,\Delta t\, k^3},
   $$

   bootstrapped by SBDF1. The implicit treatment removes the
   $\Delta t \lesssim (\Delta x)^3$ restriction an explicit method would face
   for $\partial_{xxx}$; the explicit convective term, whose advection speed
   is $6u$, needs $6\,\Delta t\, k_{\max} \max|u| \lesssim 1$, which with
   $k_{\max} = N_x/2 - 1 = 127$, $|u| \lesssim 3.3$ and
   $\Delta t = 4\times10^{-5}$ evaluates to about $0.1$.
4. *Conservation.* The $k = 0$ mode satisfies $\hat u_0^{n+1} = (4\hat u_0^n - \hat u_0^{n-1})/3$
   with $\hat u_0^1 = \hat u_0^0$, so mass $\int u\,dx$ is exact. The
   quadratic invariant $\int u^2\,dx$ and the energy are **not** exactly
   preserved: BDF2 has $|R(i\theta)| < 1$ for $\theta \ne 0$, so dispersive
   modes are weakly damped, by $1 - |R(i\theta)| \approx \theta^4/4$ per
   step with $\theta = \Delta t\, k^3$ (about $4\times10^{-4}$ per step at
   $\theta = 0.2$). A model trained on this data should see reversible
   dynamics up to that small numerical dissipation.
5. *Output cadence and rejection.* As for Allen–Cahn (frames every
   `T_save_steps` iterations, `u_bound` filter).

**Initial conditions.** The same sum-of-sines construction as Allen–Cahn
(with `max_k = 6` by default) normalised to unit standard deviation, but
**not** rescaled to $[-1, 1]$ — KdV has no preferred amplitude. As a
consequence the constant contributions of the unselected indices survive:
the mean $\bar u$ is random and typically of order one (about $0.85$ in
magnitude on average, up to $\approx 4$), and it is conserved exactly by the
scheme. A nonzero mean is a Galilean boost for KdV (frame speed $6\bar u$),
so trajectories travel at different speeds. The `u_bound` rejection filter
applies.

**Parameters** (`configs/data/kdv_1d/default.yaml`): as for
`allen_cahn_1d` without `eps`, and with `max_k = 6`.

**Output.** `(n_traj, 101, 1, Nx)`, `channel_names = ("u",)`;
`physical = {Lx, T}`, `numerical` and `provenance` as for Allen–Cahn.

**References.**

- D. J. Korteweg and G. de Vries, "On the change of form of long waves
  advancing in a rectangular canal, and on a new type of long stationary
  waves", *Philosophical Magazine* **39**, 422–443 (1895).
- N. J. Zabusky and M. D. Kruskal, "Interaction of 'solitons' in a
  collisionless plasma and the recurrence of initial states",
  *Physical Review Letters* **15**, 240–243 (1965).
